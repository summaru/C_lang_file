#include "typedef.h"
user_link* create_info(person data);
void linking_info(user_link **head, user_link **temp, user_link *node);
void userlist(user_link *buf);
void user_change(user_link *save, pcdata *info);
